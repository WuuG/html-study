# 基础知识
+ 元素 = 开始标记 + 内容 + 结束标记
+ q标签,会在内部内容上加上”“,短引用,inline --> 对SEO优化，样式设计等有所帮助
+ blackquote标签，长引用,block
+ void元素：没有实际内容的元素，可以进行简写。 如下面二者
   ``` html
   <br></br>
   <br>
   ```
+ order list -> ol, unorder list -> ul, list item -> li